[project]
name = "splithorizon"
version = "0.1.0"
description = "SplitHorizon — adversarial decision simulator for startup founders"

# Ensures `jac install` syncs litellm into .jac/venv for by llm() drafts.
[project.intents]
runtime = ["llm"]

[byllm.model]
default_model = "groq/llama-3.3-70b-versatile"
